apiVersion: v2
name: docling-serve
description: Helm chart for deploying Docling Serve with PVC-backed cache and configurable backend models.
type: application
version: "0.0.2"
# NOTE: Pin appVersion to a specific release tag or image digest in production.
# "latest" is mutable and makes rollbacks unreliable.
appVersion: "latest"
keywords:
  - docling
  - NLP
  - model-serving
  - PVC
maintainers:
  - name: Prashanth Nandavanam
    email: pn@forwardmeasure.com
